files:
  "/etc/cron.d/laravel_scheduler":
    mode: "000644"
    owner: root
    group: root
    content: |
      * * * * * root /usr/bin/flock -n /tmp/cron.lock /var/app/current/scheduler.sh
container_commands:
  01_reload_cron:
    command: "service crond reload"